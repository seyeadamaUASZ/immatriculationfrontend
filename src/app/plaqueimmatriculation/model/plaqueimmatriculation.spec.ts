import { Plaqueimmatriculation } from './plaqueimmatriculation';

describe('Plaqueimmatriculation', () => {
  it('should create an instance', () => {
    expect(new Plaqueimmatriculation()).toBeTruthy();
  });
});
